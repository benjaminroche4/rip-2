<img
    src="{{ asset('medias/images/grid-background.svg') }}?v=3"
    alt="Grid Background Image"
    aria-hidden="true"
    loading="eager"
    decoding="async"
    fetchpriority="low"
    class="absolute top-0 left-1/2 -translate-x-1/2 z-0 pointer-events-none w-full max-w-7xl h-auto {{ auth|default(false) ? '' : 'mt-18' }}"
>
