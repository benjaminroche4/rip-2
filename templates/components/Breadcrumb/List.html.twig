{# @block content The default block #}
<ol
    class="{{ 'flex items-center gap-1.5 text-sm text-gray-500 sm:gap-2.5 min-w-0 ' ~ attributes.render('class')|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</ol>
