{% extends 'base.html.twig' %}

{% block title %}{{ 'meta.companies.title'|trans }}{% endblock %}
{% block description %}{{ 'meta.companies.description'|trans }}{% endblock %}
{% block url %}{{ path('app_service_companies') }}{% endblock %}

{% block body %}
    <section class="max-w-7xl mx-auto lg:px-8 px-6 lg:py-16 py-12">
        <div class="max-w-2xl mx-auto flex flex-col justify-center text-center space-y-4">
            <h1 class="text-center font-semibold text-primary font-secondary">{{ 'companies.title'|trans|raw }}</h1>
            <h2 class="text-4xl lg:text-5xl/16 font-medium text-balance">{{ 'companies.subTitle'|trans|raw }}</h2>
        </div>
    </section>
    <section class="max-w-6xl mx-auto lg:px-8 px-6 pt-4 lg:pb-16 pb-12">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 gap-6 justify-center max-w-xl lg:max-w-4xl mx-auto">
            <div class="bg-white border border-neutral-100 rounded-2xl p-6 bg-neutral-100 transition duration-100 hover:border-primary/20 group">
                <div class="flex flex-col">
                    <div class="space-y-2">
                        <div class="flex justify-between items-center mb-6">
                            <div class="flex size-13 items-center justify-center rounded-full bg-primary/10">
                                {{ ux_icon('lucide:briefcase', {'class':'size-6 text-primary'}) }}
                            </div>
                            <div class="inline-block px-2.5 py-1 bg-primary/5 rounded-full text-xs text-primary border border-primary/10 capitalize">
                                +220 entreprises
                            </div>
                        </div>
                        <h3 class="font-semibold text-gray-900 text-xl">
                            {{ 'companies.services.1.title'|trans|raw }}
                        </h3>
                        <p class="text-neutral-800/70">
                            {{ 'companies.services.1.subTitle'|trans|raw }}
                        </p>
                    </div>
                    <div class="mt-3 flex flex-auto flex-col text-base/7 bg-neutral-100/40 rounded-lg mt-5 p-2">
                        <div class="space-y-3 text-neutral-800/90">
                            <div class="flex flex-row gap-x-2.5 items-center text-sm p-2.5">
                                {{ ux_icon('gg:check-o', {'class':'size-4 shrink-0 text-primary'}) }}
                                <p>{{ 'companies.services.1.offer.1'|trans|raw }}</p>
                            </div>
                            <div class="flex flex-row gap-x-2.5 items-center text-sm p-2.5">
                                {{ ux_icon('gg:check-o', {'class':'size-4 shrink-0 text-primary'}) }}
                                <p>{{ 'companies.services.1.offer.2'|trans|raw }}</p>
                            </div>
                            <div class="flex flex-row gap-x-2.5 items-center text-sm p-2.5">
                                {{ ux_icon('gg:check-o', {'class':'size-4 shrink-0 text-primary'}) }}
                                <p>{{ 'companies.services.1.offer.3'|trans|raw }}</p>
                            </div>
                            <div class="flex flex-row gap-x-2.5 items-center text-sm p-2.5">
                                {{ ux_icon('gg:check-o', {'class':'size-4 shrink-0 text-green-600'}) }}
                                <p>{{ 'companies.services.1.offer.4'|trans|raw }}</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="bg-white border border-neutral-100 rounded-xl p-6 bg-neutral-100 transition duration-100 hover:border-primary/20 group">
                <dl class="flex flex-col">
                    <dt class="text-base/7 font-semibold text-gray-900 text-lg">
                        <div class="mb-6 flex size-10 items-center justify-center rounded-lg bg-primary">
                            {{ ux_icon('lucide:building', {'class':'size-6 text-white'}) }}
                        </div>
                        {{ 'companies.services.2.title'|trans|raw }}
                    </dt>
                    <dd class="mt-3 flex flex-auto flex-col text-base/7">
                        <p class="flex-auto text-neutral-800/70">
                            {{ 'companies.services.2.subTitle'|trans|raw }}
                        </p>
                        <div class="mt-5 space-y-3 text-neutral-800/90">
                            <div class="flex flex-row gap-x-2.5 items-center bg-neutral-100/40 text-sm rounded-lg p-2.5">
                                {{ ux_icon('gg:check-o', {'class':'size-4 shrink-0 text-primary'}) }}
                                <p>{{ 'companies.services.2.offer.1'|trans|raw }}</p>
                            </div>
                            <div class="flex flex-row gap-x-2.5 items-center bg-neutral-100/40 text-sm rounded-lg p-2.5">
                                {{ ux_icon('gg:check-o', {'class':'size-4 shrink-0 text-primary'}) }}
                                <p>{{ 'companies.services.2.offer.2'|trans|raw }}</p>
                            </div>
                            <div class="flex flex-row gap-x-2.5 items-center bg-neutral-100/40 text-sm rounded-lg p-2.5">
                                {{ ux_icon('gg:check-o', {'class':'size-4 shrink-0 text-primary'}) }}
                                <p>{{ 'companies.services.2.offer.3'|trans|raw }}</p>
                            </div>
                            <div class="flex flex-row gap-x-2.5 items-center bg-neutral-100/40 text-sm rounded-lg p-2.5">
                                {{ ux_icon('gg:check-o', {'class':'size-4 shrink-0 text-primary'}) }}
                                <p>{{ 'companies.services.2.offer.4'|trans|raw }}</p>
                            </div>
                        </div>
                    </dd>
                </dl>
            </div>
        </div>
    </section>
    <section class="max-w-7xl mx-auto lg:px-8 px-6 lg:py-16 py-12">
        <div class="max-w-3xl mx-auto">
            <div class="pb-12 lg:pb-16">
                <h2 class="text-3xl font-medium text-pretty text-center">{{ 'companies.faq.title'|trans|raw }}</h2>
            </div>
            <div x-data="{
                    activeAccordion: '',
                    setActiveAccordion(id) {
                        this.activeAccordion = (this.activeAccordion == id) ? '' : id
                    }
                }" class="relative w-full mx-auto overflow-hidden space-y-4">
                <div x-data="{ id: $id('accordion') }" class="cursor-pointer group border border-neutral-100 bg-white rounded-lg hover:border-primary/20 transition duration-100">
                    <button @click="setActiveAccordion(id)" class="flex items-center justify-between w-full p-4 text-left select-none font-semibold gap-x-2">
                        <span>{{ 'companies.faq.question.1.title'|trans|raw }}</span>
                        <svg class="w-4 h-4 duration-200 ease-out" :class="{ 'rotate-180': activeAccordion==id }" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>
                    </button>
                    <div x-show="activeAccordion==id" x-collapse x-cloak>
                        <div class="p-4 pt-0 opacity-70 text-neutral-800">
                            {{ 'companies.faq.question.1.text'|trans|raw }}
                        </div>
                    </div>
                </div>
                <div x-data="{ id: $id('accordion') }" class="cursor-pointer group border border-neutral-100 bg-white rounded-lg hover:border-primary/20 transition duration-100">
                    <button @click="setActiveAccordion(id)" class="flex items-center justify-between w-full p-4 text-left select-none font-semibold gap-x-2">
                        <span>{{ 'companies.faq.question.2.title'|trans|raw }}</span>
                        <svg class="w-4 h-4 duration-200 ease-out" :class="{ 'rotate-180': activeAccordion==id }" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>
                    </button>
                    <div x-show="activeAccordion==id" x-collapse x-cloak>
                        <div class="p-4 pt-0 opacity-70 text-neutral-800">
                            {{ 'companies.faq.question.2.text'|trans|raw }}
                        </div>
                    </div>
                </div>
                <div x-data="{ id: $id('accordion') }" class="cursor-pointer group border border-neutral-100 bg-white rounded-lg hover:border-primary/20 transition duration-100">
                    <button @click="setActiveAccordion(id)" class="flex items-center justify-between w-full p-4 text-left select-none font-semibold gap-x-2">
                        <span>{{ 'companies.faq.question.3.title'|trans|raw }}</span>
                        <svg class="w-4 h-4 duration-200 ease-out" :class="{ 'rotate-180': activeAccordion==id }" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>
                    </button>
                    <div x-show="activeAccordion==id" x-collapse x-cloak>
                        <div class="p-4 pt-0 opacity-70 text-neutral-800">
                            {{ 'companies.faq.question.3.text'|trans|raw }}
                        </div>
                    </div>
                </div>
                <div x-data="{ id: $id('accordion') }" class="cursor-pointer group border border-neutral-100 bg-white rounded-lg hover:border-primary/20 transition duration-100">
                    <button @click="setActiveAccordion(id)" class="flex items-center justify-between w-full p-4 text-left select-none font-semibold gap-x-2">
                        <span>{{ 'companies.faq.question.4.title'|trans|raw }}</span>
                        <svg class="w-4 h-4 duration-200 ease-out" :class="{ 'rotate-180': activeAccordion==id }" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>
                    </button>
                    <div x-show="activeAccordion==id" x-collapse x-cloak>
                        <div class="p-4 pt-0 opacity-70 text-neutral-800">
                            {{ 'companies.faq.question.4.text'|trans|raw }}
                        </div>
                    </div>
                </div>
                <div x-data="{ id: $id('accordion') }" class="cursor-pointer group border border-neutral-100 bg-white rounded-lg hover:border-primary/20 transition duration-100">
                    <button @click="setActiveAccordion(id)" class="flex items-center justify-between w-full p-4 text-left select-none font-semibold gap-x-2">
                        <span>{{ 'companies.faq.question.5.title'|trans|raw }}</span>
                        <svg class="w-4 h-4 duration-200 ease-out" :class="{ 'rotate-180': activeAccordion==id }" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>
                    </button>
                    <div x-show="activeAccordion==id" x-collapse x-cloak>
                        <div class="p-4 pt-0 opacity-70 text-neutral-800">
                            {{ 'companies.faq.question.5.text'|trans|raw }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    {{ component('ctaFooter') }}
{% endblock %}
