{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'admin.tools.documents.request.title'|trans }}{% endblock %}

{% block page_header %}
    <twig:Admin:PageHeader
        title="{{ 'admin.tools.documents.request.title'|trans }}"
        subtitle="{{ 'admin.tools.documents.request.subtitle'|trans }}"
    >
        <a
            href="{{ path('admin_tools_documents', {adminPrefix: adminPrefix}) }}"
            class="inline-flex shrink-0 items-center gap-1.5 text-sm text-gray-500 transition-colors hover:text-gray-900"
        >
            {{ ux_icon('lucide:arrow-left', {class: 'size-4', 'aria-hidden': 'true'}) }}
            {{ 'admin.tools.documents.request.backToDocuments'|trans }}
        </a>
    </twig:Admin:PageHeader>
{% endblock %}

{% block body %}
    <div data-testid="tools-documents-request-page">
        <twig:Admin:DocumentRequestForm :adminPrefix="adminPrefix" />
    </div>
{% endblock %}
