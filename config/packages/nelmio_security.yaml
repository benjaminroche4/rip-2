nelmio_security:
    # prevents framing of the entire site
    clickjacking:
        # First match wins: the deposited-file routes are previewed in a
        # same-origin iframe modal (deposit page + admin file module), the
        # rest of the site stays DENY.
        paths:
            '^/(fr|en)/(depot-de-pieces|document-upload)/(fichier|file)/\d+$': SAMEORIGIN
            '^/(fr|en)/[a-zA-Z0-9_-]+/admin/(dossiers|files)/DS-\d{6}/(fichiers|files)/\d+$': SAMEORIGIN
            '^/.*': DENY

    # disables content type sniffing for script resources
    content_type:
        nosniff: true

    # Send a full URL in the `Referer` header when performing a same-origin request,
    # only send the origin of the document to secure destination (HTTPS->HTTPS),
    # and send no header to a less secure destination (HTTPS->HTTP).
    # If `strict-origin-when-cross-origin` is not supported, use `no-referrer` policy,
    # no referrer information is sent along with requests.
    referrer_policy:
        enabled: true
        policies:
            - 'no-referrer'
            - 'strict-origin-when-cross-origin'

    # CSP — enabled in REPORT-ONLY mode: the browser reports violations but
    # never blocks, so shipping this cannot break the site. The current stack
    # still relies on `'unsafe-inline'`/`'unsafe-eval'` on script-src (Stimulus
    # / Turbo bootstrap + EasyAdmin). Address the reported violations one by one
    # (switch to nonces, externalise inline scripts), then move these directives
    # under an `enforce:` block once the report stream goes quiet.
    csp:
        enabled: true
        report:
            default-src: ["'self'"]
            script-src:
                - "'self'"
                - "'unsafe-eval'"
                - "'unsafe-inline'"
                - "https://www.googletagmanager.com"
                - "https://maps.googleapis.com"
            style-src:
                - "'self'"
                - "'unsafe-inline'"
                - "https://fonts.googleapis.com"
            img-src:
                - "'self'"
                - "data:"
                - "https://cdn.sanity.io"
                - "https://maps.googleapis.com"
                - "https://maps.gstatic.com"
                - "https://*.googleusercontent.com"
                - "https://www.googletagmanager.com"
                - "https://www.google-analytics.com"
            font-src:
                - "'self'"
                - "data:"
                - "https://fonts.gstatic.com"
            connect-src:
                - "'self'"
                - "https://*.sanity.io"
                - "https://maps.googleapis.com"
                - "https://www.google-analytics.com"
                - "https://region1.google-analytics.com"
            frame-src:
                - "'self'"
                - "https://www.youtube.com"
                - "https://www.youtube-nocookie.com"
                - "https://www.google.com"
            object-src: ["'none'"]
            base-uri: ["'self'"]
            frame-ancestors: ["'self'"]

# Strict-Transport-Security — prod only (the bundle wires this at container
# compile time, no runtime toggle possible). Locked in for 1 year, includes
# subdomains, opted into Chrome's HSTS preload list (https://hstspreload.org).
#
# Dev + test run over plain http, so the block does not exist there. To run
# APP_ENV=prod LOCALLY, serve over https (symfony server:ca:install once,
# then `symfony serve` without --no-tls): the redirect never triggers and the
# HSTS header is harmless since local https actually works.
when@prod:
    nelmio_security:
        forced_ssl:
            hsts_max_age: 31536000           # 1 year
            hsts_subdomains: true
            hsts_preload: true
